module.exports = function(self, request, response, item) {
    rquest.flash('info', +'deleted ' + self.name + ' ' + item._id);
    response.render(self.name + '/destroy.html', {
        item: item
    });
}