module.exports = function(shades, r, c) {

    var canvas = new Canvas(c, r);
    var ctx = canvas.getContext('2d');

    var id = ctx.createImageData(c, r);
    shades.forEach(function(shade, n) {
        var p = 4 * n;
        id.data[n] = id.data[n + 1] = id.data[n + 2] = shade;
        id.data[n + 3] = 255;
    });
    ctx.putImageData(id);

    return canvas;
}