var Grid = require('./grid');

function Walker(){
    this.grid = false;
}

Walker.prototype = {
    init: function(i, j, props){
        this.grid = new Grid(i, j, props);
    }
}

module.exports = Walker;