module.exports = {
    mapreduce: "randomwalks",
    out: "randomwalks_coverage",
    map: function() {
        emit(this.last, {count: 1});
    }.toString(),
   // verbose: true,
    reduce: function(key, values) {
        var count = 0;
        var ret = {count: 0}
        values.forEach(function(v) { ret.count += v.count;});
        return ret;
    }.toString()
}