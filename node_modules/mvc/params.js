var string_utils = require('util/string');
var inflect      = require('inflect');

module.exports = function(name, params){
    if (!params){
        params = {};
    }
    params.controller = name;
    params.plural = inflect(name);
    params.controller_title = string_utils.title_case(name);
    
    return params;
}