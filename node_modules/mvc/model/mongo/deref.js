
var util = require('util');

module.exports = function(dbRef, callback) {
    var mm = require(MVC_MODELS);
  //  console.log('MVC MODELS: ', MVC_MODELS);
    var self = this;
   // console.log(__filename, util.inspect(mm));

    mm.model(dbRef.namespace, function(err, model) {
     //   console.log(__filename, 'model for ', dbRef.namespace, ': ', 'model: ', util.inspect(model));
        model.get(dbRef.oid, callback);
    });

}