// note- options is a legacy property, and generates an error if present.
// this function returns a cursor. 
module.exports = function(query, fields, callback, options) {

    if (this.config.coll) {
        return this.config.coll.findOne(query, fields, callback);

    } else {
        throw (new Error('No Connection'));
    }
}