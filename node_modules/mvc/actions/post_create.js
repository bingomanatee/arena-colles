module.exports = function(self, request, response, result) {
    request.flash('info', self.name + ' created');
    if (!request.hasOwnProperty('params')){
        request.params = {};
    }
    
    request.params.id = result._id;
    
    self.show(request, response);
    
}