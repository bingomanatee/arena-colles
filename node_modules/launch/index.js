var path = require('path');

module.exports.init = function(app, callback) {
    /**
     * @TODO: move constants to config file
     */
    
    global._ = require('underscore');
    global.__ = require('underscore.string');
    callback(null, app);
};